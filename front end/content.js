// The content script portion of the Chrome extension is able to interact with the webpages the user has loaded
console.log("Content script loaded");
function extractText() {
    return document.body.innerText;
  }
  

 

